export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "NYT Headliner",
  description: "A small app to search NYT headlines.",
  navItems: [],
  navMenuItems: [],
  links: {},
};
